# 模糊打码遮罩功能实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 OpenScreen 视频编辑器添加模糊打码遮罩功能，作为标注的一种新类型，支持高斯模糊、纯色遮挡和强力模糊三种效果。

**架构：** 扩展现有的 `AnnotationRegion` 类型，添加 `blurData` 字段；在预览层使用 CSS `backdrop-filter` 渲染；在导出层使用 Canvas 2D API 渲染模糊效果；复用现有的拖拽/缩放组件和设置面板框架。

**技术栈：** TypeScript, React, Tailwind CSS, Canvas 2D API, CSS backdrop-filter

---

## 文件结构

| 文件 | 职责 | 操作 |
|------|------|------|
| `src/components/video-editor/types.ts` | 类型定义 | 修改 |
| `src/components/video-editor/AnnotationOverlay.tsx` | 预览渲染 | 修改 |
| `src/components/video-editor/AnnotationSettingsPanel.tsx` | 设置面板 UI | 修改 |
| `src/components/video-editor/VideoEditor.tsx` | 状态管理 | 修改 |
| `src/lib/exporter/frameRenderer.ts` | 导出渲染 | 修改 |
| `src/i18n/locales/en/settings.json` | 英文翻译 | 修改 |
| `src/i18n/locales/zh-CN/settings.json` | 中文翻译 | 修改 |
| `src/i18n/locales/es/settings.json` | 西班牙语翻译 | 修改 |

---

## 任务 1：类型定义

**文件：**
- 修改：`src/components/video-editor/types.ts`

- [ ] **步骤 1：添加 BlurEffectType 和 BlurData 类型**

在 `types.ts` 中，在 `FigureData` 接口之后添加：

```typescript
// === Blur Mask Types ===

export type BlurEffectType = "gaussian" | "solid" | "heavy";

export interface BlurData {
	effectType: BlurEffectType;
	intensity: number; // 1-30
	feathering: number; // 0-20px
	solidColor: string; // hex color, used for "solid" type
}

export const DEFAULT_BLUR_DATA: BlurData = {
	effectType: "gaussian",
	intensity: 15,
	feathering: 0,
	solidColor: "#000000",
};

export const DEFAULT_BLUR_SIZE: AnnotationSize = {
	width: 25,
	height: 20,
};
```

- [ ] **步骤 2：扩展 AnnotationType**

将：
```typescript
export type AnnotationType = "text" | "image" | "figure";
```

改为：
```typescript
export type AnnotationType = "text" | "image" | "figure" | "blur";
```

- [ ] **步骤 3：扩展 AnnotationRegion 接口**

在 `AnnotationRegion` 接口中添加 `blurData` 字段：

```typescript
export interface AnnotationRegion {
	id: string;
	startMs: number;
	endMs: number;
	type: AnnotationType;
	content: string;
	textContent?: string;
	imageContent?: string;
	position: AnnotationPosition;
	size: AnnotationSize;
	style: AnnotationTextStyle;
	zIndex: number;
	figureData?: FigureData;
	blurData?: BlurData; // 新增
}
```

- [ ] **步骤 4：运行类型检查**

运行：`npm run build-vite`
预期：无类型错误

- [ ] **步骤 5：Commit**

```bash
git add src/components/video-editor/types.ts
git commit -m "feat: add BlurData type definitions for blur mask feature"
```

---

## 任务 2：预览渲染

**文件：**
- 修改：`src/components/video-editor/AnnotationOverlay.tsx`

- [ ] **步骤 1：添加 blur case 到 renderContent 函数**

在 `renderContent()` 函数的 `switch` 语句中，在 `case "figure"` 之后添加 `case "blur"`：

```tsx
case "blur":
	if (!annotation.blurData) {
		return (
			<div className="w-full h-full flex items-center justify-center text-slate-400 text-sm">
				No blur data
			</div>
		);
	}
	return renderBlurOverlay();
```

- [ ] **步骤 2：添加 renderBlurOverlay 函数**

在 `AnnotationOverlay` 组件内部，`renderContent` 函数之前添加：

```tsx
const renderBlurOverlay = () => {
	const { effectType, intensity, feathering, solidColor } = annotation.blurData!;

	// 羽化遮罩（使用四边渐变实现，与导出渲染一致）
	const featherPx = `${feathering}px`;
	const featherGradient = feathering > 0
		? `
				linear-gradient(to right, black ${featherPx}, transparent ${featherPx}, transparent calc(100% - ${featherPx}), black calc(100% - ${featherPx})),
				linear-gradient(to bottom, black ${featherPx}, transparent ${featherPx}, transparent calc(100% - ${featherPx}), black calc(100% - ${featherPx}))
			`
		: undefined;

	switch (effectType) {
		case "gaussian":
			return (
				<div
					className="w-full h-full"
					style={{
						backdropFilter: `blur(${intensity}px)`,
						WebkitBackdropFilter: `blur(${intensity}px)`,
						maskImage: featherGradient,
						WebkitMaskImage: featherGradient,
						maskComposite: "intersect",
						WebkitMaskComposite: "source-in",
					}}
				/>
			);
		case "solid":
			return (
				<div
					className="w-full h-full"
					style={{
						backgroundColor: solidColor,
						maskImage: featherGradient,
						WebkitMaskImage: featherGradient,
						maskComposite: "intersect",
						WebkitMaskComposite: "source-in",
					}}
				/>
			);
		case "heavy":
			return (
				<div
					className="w-full h-full"
					style={{
						backdropFilter: `blur(${intensity * 1.5}px) saturate(0.3)`,
						WebkitBackdropFilter: `blur(${intensity * 1.5}px) saturate(0.3)`,
						maskImage: featherGradient,
						WebkitMaskImage: featherGradient,
						maskComposite: "intersect",
						WebkitMaskComposite: "source-in",
					}}
				/>
			);
	}
};
```

- [ ] **步骤 3：更新 className 条件**

在 `Rnd` 组件内部的 `div` 中，添加 `blur` 类型的条件：

将：
```tsx
className={cn(
	"w-full h-full rounded-lg",
	annotation.type === "text" && "bg-transparent",
	annotation.type === "image" && "bg-transparent",
	annotation.type === "figure" && "bg-transparent",
	isSelected && "shadow-lg",
)}
```

改为：
```tsx
className={cn(
	"w-full h-full rounded-lg",
	annotation.type === "text" && "bg-transparent",
	annotation.type === "image" && "bg-transparent",
	annotation.type === "figure" && "bg-transparent",
	annotation.type === "blur" && "bg-transparent",
	isSelected && "shadow-lg",
)}
```

- [ ] **步骤 4：运行类型检查**

运行：`npm run build-vite`
预期：无类型错误

- [ ] **步骤 5：Commit**

```bash
git add src/components/video-editor/AnnotationOverlay.tsx
git commit -m "feat: add blur mask preview rendering in AnnotationOverlay"
```

---

## 任务 3：国际化翻译

**文件：**
- 修改：`src/i18n/locales/en/settings.json`
- 修改：`src/i18n/locales/zh-CN/settings.json`
- 修改：`src/i18n/locales/es/settings.json`

- [ ] **步骤 1：添加英文翻译**

在 `en/settings.json` 的 `annotation` 对象中添加：

```json
"typeBlur": "Blur",
"blurType": "Blur Type",
"blurGaussian": "Gaussian",
"blurSolid": "Solid Color",
"blurHeavy": "Heavy Blur",
"blurIntensity": "Blur Intensity",
"blurFeathering": "Feathering",
"blurSolidColor": "Solid Color",
"blurTipIntensity": "Higher values create stronger blur",
"blurTipFeathering": "Soften the edges of the blur area"
```

- [ ] **步骤 2：添加中文翻译**

在 `zh-CN/settings.json` 的 `annotation` 对象中添加：

```json
"typeBlur": "模糊",
"blurType": "模糊类型",
"blurGaussian": "高斯模糊",
"blurSolid": "纯色遮挡",
"blurHeavy": "强力模糊",
"blurIntensity": "模糊强度",
"blurFeathering": "羽化程度",
"blurSolidColor": "遮挡颜色",
"blurTipIntensity": "值越大模糊效果越强",
"blurTipFeathering": "柔化模糊区域边缘"
```

- [ ] **步骤 3：添加西班牙语翻译**

在 `es/settings.json` 的 `annotation` 对象中添加：

```json
"typeBlur": "Desenfoque",
"blurType": "Tipo de desenfoque",
"blurGaussian": "Gaussiano",
"blurSolid": "Color sólido",
"blurHeavy": "Desenfoque intenso",
"blurIntensity": "Intensidad",
"blurFeathering": "Difuminado de bordes",
"blurSolidColor": "Color sólido",
"blurTipIntensity": "Valores más altos crean un desenfoque más fuerte",
"blurTipFeathering": "Suaviza los bordes del área desenfocada"
```

- [ ] **步骤 4：运行国际化检查**

运行：`npm run i18n:check`
预期：所有语言包键一致

- [ ] **步骤 5：Commit**

```bash
git add src/i18n/locales/*/settings.json
git commit -m "feat(i18n): add blur mask translations for en/zh-CN/es"
```

---

## 任务 4：设置面板 UI

**文件：**
- 修改：`src/components/video-editor/AnnotationSettingsPanel.tsx`

- [ ] **步骤 1：导入 EyeOff 图标和 BlurData 类型**

在文件顶部的导入语句中：
- 从 `lucide-react` 添加 `EyeOff` 图标
- 从 `./types` 添加 `BlurData` 类型

```tsx
import {
	// ...existing imports
	EyeOff,
} from "lucide-react";
import type {
	// ...existing type imports
	BlurData,
} from "./types";
```

- [ ] **步骤 2：添加 Blur Tab 到 TabsList**

在 `TabsList` 中，将 `grid-cols-3` 改为 `grid-cols-4`，并添加模糊 Tab：

```tsx
<TabsList className="mb-4 bg-white/5 border border-white/5 p-1 w-full grid grid-cols-4 h-auto rounded-xl">
	<TabsTrigger value="text" ...>
		<Type className="w-4 h-4" />
		{t("annotation.typeText")}
	</TabsTrigger>
	<TabsTrigger value="image" ...>
		<ImageIcon className="w-4 h-4" />
		{t("annotation.typeImage")}
	</TabsTrigger>
	<TabsTrigger value="figure" ...>
		<svg className="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
			<path d="M4 12h16m0 0l-6-6m6 6l-6 6" strokeLinecap="round" strokeLinejoin="round" />
		</svg>
		{t("annotation.typeArrow")}
	</TabsTrigger>
	<TabsTrigger
		value="blur"
		className="data-[state=active]:bg-[#34B27B] data-[state=active]:text-white text-slate-400 py-2 rounded-lg transition-all gap-2"
	>
		<EyeOff className="w-4 h-4" />
		{t("annotation.typeBlur")}
	</TabsTrigger>
</TabsList>
```

- [ ] **步骤 3：添加 BlurData 变更处理器到 props**

在 `AnnotationSettingsPanelProps` 接口中添加：

```tsx
interface AnnotationSettingsPanelProps {
	// ...existing props
	onBlurDataChange?: (blurData: BlurData) => void;
}
```

并在函数参数中解构：

```tsx
export function AnnotationSettingsPanel({
	// ...existing params
	onBlurDataChange,
}: AnnotationSettingsPanelProps) {
```

- [ ] **步骤 4：添加 Blur TabsContent**

在 `TabsContent value="figure"` 之后添加：

```tsx
<TabsContent value="blur" className="mt-0 space-y-4">
	{annotation.blurData && onBlurDataChange && (
		<>
			{/* Blur Type Selector */}
			<div>
				<label className="text-xs font-medium text-slate-200 mb-2 block">
					{t("annotation.blurType")}
				</label>
				<div className="grid grid-cols-3 gap-2">
					{(["gaussian", "solid", "heavy"] as BlurEffectType[]).map((type) => (
						<button
							key={type}
							onClick={() => {
								const newBlurData: BlurData = {
									...annotation.blurData!,
									effectType: type,
								};
								onBlurDataChange(newBlurData);
							}}
							className={cn(
								"h-10 rounded-lg border text-xs font-medium transition-all",
								annotation.blurData?.effectType === type
									? "bg-[#34B27B] border-[#34B27B] text-white"
									: "bg-white/5 border-white/10 text-slate-400 hover:bg-white/10 hover:border-white/20",
							)}
						>
							{t(`annotation.blur${type.charAt(0).toUpperCase() + type.slice(1)}` as const)}
						</button>
					))}
				</div>
			</div>

			{/* Intensity Slider (for gaussian and heavy) */}
			{annotation.blurData.effectType !== "solid" && (
				<div>
					<label className="text-xs font-medium text-slate-200 mb-2 block">
						{t("annotation.blurIntensity")}: {annotation.blurData.intensity}
					</label>
					<Slider
						value={[annotation.blurData.intensity]}
						onValueChange={([value]) => {
							const newBlurData: BlurData = {
								...annotation.blurData!,
								intensity: value,
							};
							onBlurDataChange(newBlurData);
						}}
						min={1}
						max={30}
						step={1}
						className="w-full"
					/>
					<p className="text-[10px] text-slate-500 mt-1">{t("annotation.blurTipIntensity")}</p>
				</div>
			)}

			{/* Feathering Slider */}
			<div>
				<label className="text-xs font-medium text-slate-200 mb-2 block">
					{t("annotation.blurFeathering")}: {annotation.blurData.feathering}px
				</label>
				<Slider
					value={[annotation.blurData.feathering]}
					onValueChange={([value]) => {
						const newBlurData: BlurData = {
							...annotation.blurData!,
							feathering: value,
						};
						onBlurDataChange(newBlurData);
					}}
					min={0}
					max={20}
					step={1}
					className="w-full"
				/>
				<p className="text-[10px] text-slate-500 mt-1">{t("annotation.blurTipFeathering")}</p>
			</div>

			{/* Solid Color Picker (only for solid type) */}
			{annotation.blurData.effectType === "solid" && (
				<div>
					<label className="text-xs font-medium text-slate-200 mb-2 block">
						{t("annotation.blurSolidColor")}
					</label>
					<Popover>
						<PopoverTrigger asChild>
							<Button
								variant="outline"
								className="w-full h-10 justify-start gap-2 bg-white/5 border-white/10 hover:bg-white/10"
							>
								<div
									className="w-5 h-5 rounded-full border border-white/20"
									style={{ backgroundColor: annotation.blurData?.solidColor || "#000000" }}
								/>
								<span className="text-xs text-slate-300 truncate flex-1 text-left">
									{annotation.blurData?.solidColor || "#000000"}
								</span>
								<ChevronDown className="h-3 w-3 opacity-50" />
							</Button>
						</PopoverTrigger>
						<PopoverContent className="w-[260px] p-3 bg-[#1a1a1c] border border-white/10 rounded-xl shadow-xl">
							<Block
								color={annotation.blurData?.solidColor || "#000000"}
								colors={colorPalette}
								onChange={(color) => {
									const newBlurData: BlurData = {
										...annotation.blurData!,
										solidColor: color.hex,
									};
									onBlurDataChange(newBlurData);
								}}
								style={{ borderRadius: "8px" }}
							/>
						</PopoverContent>
					</Popover>
				</div>
			)}
		</>
	)}
</TabsContent>
```

- [ ] **步骤 5：导入 BlurEffectType 类型**

在类型导入中添加 `BlurEffectType`：

```tsx
import type { AnnotationRegion, AnnotationType, ArrowDirection, FigureData, BlurData, BlurEffectType } from "./types";
```

- [ ] **步骤 6：运行类型检查**

运行：`npm run build-vite`
预期：无类型错误

- [ ] **步骤 7：Commit**

```bash
git add src/components/video-editor/AnnotationSettingsPanel.tsx
git commit -m "feat: add blur mask settings UI in AnnotationSettingsPanel"
```

---

## 任务 5：状态管理

**文件：**
- 修改：`src/components/video-editor/VideoEditor.tsx`

- [ ] **步骤 1：导入新类型**

在文件顶部的类型导入中添加 `BlurData` 和 `DEFAULT_BLUR_DATA`：

```tsx
import {
	// ...existing imports
	BlurData,
	DEFAULT_BLUR_DATA,
} from "./types";
```

- [ ] **步骤 2：扩展 handleAnnotationTypeChange 函数**

找到 `handleAnnotationTypeChange` 函数，将：

```tsx
const handleAnnotationTypeChange = useCallback(
	(id: string, type: AnnotationRegion["type"]) => {
		pushState((prev) => ({
			annotationRegions: prev.annotationRegions.map((region) => {
				if (region.id !== id) return region;
				const updatedRegion = { ...region, type };
				if (type === "text") {
					updatedRegion.content = region.textContent || "Enter text...";
				} else if (type === "image") {
					updatedRegion.content = region.imageContent || "";
				} else if (type === "figure") {
					updatedRegion.content = "";
					if (!region.figureData) {
						updatedRegion.figureData = { ...DEFAULT_FIGURE_DATA };
					}
				}
				return updatedRegion;
			}),
		}));
	},
	[pushState],
);
```

改为：

```tsx
const handleAnnotationTypeChange = useCallback(
	(id: string, type: AnnotationRegion["type"]) => {
		pushState((prev) => ({
			annotationRegions: prev.annotationRegions.map((region) => {
				if (region.id !== id) return region;
				const updatedRegion = { ...region, type };
				
				switch (type) {
					case "text":
						updatedRegion.content = region.textContent || "Enter text...";
						delete updatedRegion.blurData;
						break;
					case "image":
						updatedRegion.content = region.imageContent || "";
						delete updatedRegion.blurData;
						break;
					case "figure":
						updatedRegion.content = "";
						if (!region.figureData) {
							updatedRegion.figureData = { ...DEFAULT_FIGURE_DATA };
						}
						delete updatedRegion.blurData;
						break;
					case "blur":
						updatedRegion.content = "";
						if (!region.blurData) {
							updatedRegion.blurData = { ...DEFAULT_BLUR_DATA };
						}
						delete updatedRegion.figureData;
						break;
				}
				
				return updatedRegion;
			}),
		}));
	},
	[pushState],
);
```

- [ ] **步骤 3：添加 handleAnnotationBlurDataChange 函数**

在 `handleAnnotationFigureDataChange` 函数之后添加：

```tsx
const handleAnnotationBlurDataChange = useCallback(
	(id: string, blurData: BlurData) => {
		pushState((prev) => ({
			annotationRegions: prev.annotationRegions.map((region) =>
				region.id === id ? { ...region, blurData } : region,
			),
		}));
	},
	[pushState],
);
```

- [ ] **步骤 4：传递 onBlurDataChange 到 SettingsPanel**

找到 `AnnotationSettingsPanel` 组件的调用位置，添加 `onBlurDataChange` prop：

```tsx
<AnnotationSettingsPanel
	annotation={selectedAnnotation}
	totalAnnotations={annotationRegions.length}
	onContentChange={(content) => handleAnnotationContentChange(selectedAnnotationId, content)}
	onTypeChange={(type) => handleAnnotationTypeChange(selectedAnnotationId, type)}
	onStyleChange={(style) => handleAnnotationStyleChange(selectedAnnotationId, style)}
	onFigureDataChange={(figureData) => handleAnnotationFigureDataChange(selectedAnnotationId, figureData)}
	onBlurDataChange={(blurData) => handleAnnotationBlurDataChange(selectedAnnotationId, blurData)}
	onZIndexChange={(action) => handleAnnotationZIndexChange(selectedAnnotationId, action)}
	onDelete={() => handleAnnotationDelete(selectedAnnotationId)}
	onDuplicate={() => handleAnnotationDuplicate(selectedAnnotationId)}
/>
```

- [ ] **步骤 5：运行类型检查**

运行：`npm run build-vite`
预期：无类型错误

- [ ] **步骤 6：Commit**

```bash
git add src/components/video-editor/VideoEditor.tsx
git commit -m "feat: add blur mask state management in VideoEditor"
```

---

## 任务 6：导出渲染

**文件：**
- 修改：`src/lib/exporter/frameRenderer.ts`

- [ ] **步骤 1：导入 BlurData 类型**

在文件顶部的导入中添加 `BlurData`：

```tsx
import type {
	// ...existing imports
	BlurData,
} from "@/components/video-editor/types";
```

- [ ] **步骤 2：添加 renderBlurMask 方法**

在 `FrameRenderer` 类中，`renderHighlightMask` 方法之后添加：

```tsx
/**
 * Renders a blur mask on the canvas.
 * Uses temporary canvas for blur processing with feathering support.
 */
private renderBlurMask(
	ctx: CanvasRenderingContext2D,
	annotation: AnnotationRegion,
	canvasWidth: number,
	canvasHeight: number,
): void {
	const { position, size, blurData } = annotation;
	if (!blurData) return;

	const x = (position.x / 100) * canvasWidth;
	const y = (position.y / 100) * canvasHeight;
	const width = (size.width / 100) * canvasWidth;
	const height = (size.height / 100) * canvasHeight;
	const { effectType, intensity, feathering, solidColor } = blurData;

	// 保存当前画布状态
	ctx.save();

	// 创建临时画布用于模糊处理
	const tempCanvas = document.createElement("canvas");
	tempCanvas.width = canvasWidth;
	tempCanvas.height = canvasHeight;
	const tempCtx = tempCanvas.getContext("2d")!;
	tempCtx.drawImage(ctx.canvas, 0, 0);

	// 创建模糊区域画布
	const blurCanvas = document.createElement("canvas");
	blurCanvas.width = Math.ceil(width);
	blurCanvas.height = Math.ceil(height);
	const blurCtx = blurCanvas.getContext("2d")!;

	// 从临时画布复制模糊区域内容
	blurCtx.drawImage(tempCanvas, -x, -y);

	// 根据类型应用模糊效果
	switch (effectType) {
		case "gaussian":
			blurCtx.filter = `blur(${intensity}px)`;
			blurCtx.drawImage(blurCanvas, 0, 0);
			blurCtx.filter = "none";
			break;

		case "solid":
			blurCtx.fillStyle = solidColor;
			blurCtx.fillRect(0, 0, width, height);
			break;

		case "heavy":
			blurCtx.filter = `blur(${intensity * 1.5}px) saturate(0.3)`;
			blurCtx.drawImage(blurCanvas, 0, 0);
			blurCtx.filter = "none";
			break;
	}

	// 应用羽化遮罩（使用与预览一致的线性渐变方法）
	if (feathering > 0) {
		const feather = Math.min(feathering, Math.min(width, height) / 2);

		// 创建羽化遮罩画布
		const maskCanvas = document.createElement("canvas");
		maskCanvas.width = Math.ceil(width);
		maskCanvas.height = Math.ceil(height);
		const maskCtx = maskCanvas.getContext("2d")!;

		// 填充白色基础
		maskCtx.fillStyle = "white";
		maskCtx.fillRect(0, 0, width, height);

		// 四边渐变（模拟 CSS linear-gradient 的效果）
		maskCtx.globalCompositeOperation = "destination-out";

		// 左边渐变
		const leftGradient = maskCtx.createLinearGradient(0, 0, feather, 0);
		leftGradient.addColorStop(0, "rgba(0,0,0,1)");
		leftGradient.addColorStop(1, "rgba(0,0,0,0)");
		maskCtx.fillStyle = leftGradient;
		maskCtx.fillRect(0, 0, feather, height);

		// 右边渐变
		const rightGradient = maskCtx.createLinearGradient(width - feather, 0, width, 0);
		rightGradient.addColorStop(0, "rgba(0,0,0,0)");
		rightGradient.addColorStop(1, "rgba(0,0,0,1)");
		maskCtx.fillStyle = rightGradient;
		maskCtx.fillRect(width - feather, 0, feather, height);

		// 上边渐变
		const topGradient = maskCtx.createLinearGradient(0, 0, 0, feather);
		topGradient.addColorStop(0, "rgba(0,0,0,1)");
		topGradient.addColorStop(1, "rgba(0,0,0,0)");
		maskCtx.fillStyle = topGradient;
		maskCtx.fillRect(0, 0, width, feather);

		// 下边渐变
		const bottomGradient = maskCtx.createLinearGradient(0, height - feather, 0, height);
		bottomGradient.addColorStop(0, "rgba(0,0,0,0)");
		bottomGradient.addColorStop(1, "rgba(0,0,0,1)");
		maskCtx.fillStyle = bottomGradient;
		maskCtx.fillRect(0, height - feather, width, feather);

		// 应用遮罩到模糊区域
		blurCtx.globalCompositeOperation = "destination-in";
		blurCtx.drawImage(maskCanvas, 0, 0);
		blurCtx.globalCompositeOperation = "source-over";
	}

	// 将处理后的模糊区域绘制回主画布
	ctx.drawImage(blurCanvas, x, y);

	ctx.restore();
}
```

- [ ] **步骤 3：在 renderFrame 方法中调用 renderBlurMask**

在 `renderFrame` 方法中，找到 `renderAnnotations` 调用的位置，在其**之前**添加模糊渲染：

```tsx
// Render blur masks (needs to be before renderAnnotations to access composed canvas)
if (
	this.config.annotationRegions &&
	this.config.annotationRegions.length > 0 &&
	this.compositeCtx
) {
	const activeBlurAnnotations = this.config.annotationRegions.filter(
		(ann) =>
			ann.type === "blur" &&
			ann.blurData &&
			timeMs >= ann.startMs &&
			timeMs < ann.endMs,
	);

	for (const blurAnnotation of activeBlurAnnotations) {
		this.renderBlurMask(
			this.compositeCtx,
			blurAnnotation,
			this.config.width,
			this.config.height,
		);
	}
}
```

这段代码应该放在 `this.compositeWithShadows(webcamFrame);` 之后，`renderAnnotations` 调用之前。

- [ ] **步骤 4：运行类型检查**

运行：`npm run build-vite`
预期：无类型错误

- [ ] **步骤 5：Commit**

```bash
git add src/lib/exporter/frameRenderer.ts
git commit -m "feat: add blur mask export rendering in frameRenderer"
```

---

## 任务 7：验证与测试

- [ ] **步骤 1：运行完整构建**

运行：`npm run build-vite`
预期：无错误

- [ ] **步骤 2：运行 lint 检查**

运行：`npm run lint`
预期：无错误

- [ ] **步骤 3：运行测试**

运行：`npm run test`
预期：所有测试通过

- [ ] **步骤 4：启动开发服务器进行手动测试**

运行：`npm run dev`

手动测试项目：
1. 创建一个标注，切换到"模糊"类型
2. 测试三种模糊类型（高斯、纯色、强力）
3. 调整模糊强度和羽化程度
4. 拖拽和缩放模糊区域
5. 导出视频，验证模糊效果正确渲染

- [ ] **步骤 5：Final Commit**

```bash
git add -A
git commit -m "feat: complete blur mask feature implementation"
```

---

## 验收标准

- [ ] 三种模糊类型（高斯、纯色、强力）在编辑器预览中正确显示
- [ ] 模糊强度和羽化参数实时生效
- [ ] 纯色遮挡的颜色选择器正常工作
- [ ] 模糊区域可拖拽和缩放
- [ ] 导出的视频中模糊效果正确应用
- [ ] 预览和导出的羽化效果一致
- [ ] 类型切换时数据正确处理
- [ ] 保存/加载项目时模糊数据正确保留
- [ ] 撤销/重做功能正常
- [ ] 复制模糊标注功能正常
- [ ] 国际化翻译完整（en、zh-CN、es）
